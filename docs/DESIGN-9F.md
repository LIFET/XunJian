# 9F 黑白编辑台 · 已确认构图

用户以“做出来看看”批准 `.impeccable/mocks/9f-editorial-desk.png` 的原生实现。9E 不再是视觉依据。

THESIS：查找与阅读同处一个平整工作面；取消常驻彩色轨、页面套框和搜索页大标题。
OWN-WORLD：白、冰灰、石墨；系统无衬线字体；细分隔线、原生搜索和菜单、无卡片式结果。
STORY：顶部切换搜索/资料集/最近，通栏搜索，选择左侧结果，在右侧直接阅读或查看信息。
FIRST VIEWPORT：紧凑原生标题栏，58–64pt 搜索带；结果栏约 1/3，阅读约 2/3；阅读区单行文件标题、阅读/信息、打开和关闭。窄窗收起辅助栏，明确按钮恢复。
FORM：用户锁定并批准 Editorial Desk 构图；本轮未执行 concept-roll，明确记录这一流程偏差，不伪造 seed、不冒用被否决的 9E 记录，也不重新抽选用户已经确认的方向。视觉验收依据为批准 comp 及其 sidecar 中的完整质量标准。原生 macOS，不适用网页或 iPhone 的导航规范。
FINISH：unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## 范围及验收

Shell、搜索、阅读、资料集、最近、设置和共享控件采用同一语言。保留查询、选择、预览挂载、资料集上下文、设置草稿与真实文件操作。示意图中的正文/建筑仅为合成文档，不写入真实用户库。

检查编译、焦点/选择/网格列数回归、明暗/宽窄原生布局；打开独立 Debug 构建展示。保留数据、分支、版本和其他修改；不提交、不推送、不发布、不替换安装版、不调用真实 OAuth/AI。
