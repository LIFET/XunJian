# 寻简开发计划

## 0.1.8（build 9）发布

用户授权提交、推送、更新README并加入软件截图，最后发布。范围为当前8V–9M积累改动；保留本地设计过程文件，不公开私人截图。发布流程：演示数据实拍→完整离线回归及大库门禁→三架构签名Archive/DMG→Apple公证→staple/Gatekeeper→GitHub资产与Sparkle签名更新验证。

- [x] README更新下载说明、资料集/查重说明，加入搜索阅读完整窗口、资料集和设置页面三张实际截图。
- [x] 工程与XcodeGen版本统一为0.1.8/build9。
- [x] 首轮全量发现OAuth取消后重启轮询竞态；限制登录准备期间启动轮询，登录就绪后统一恢复，加入受控挂起回归。OAuth连续三轮通过；最终主工程529项（4项条件跳过）与OAuth Process39项均0失败。
- [x] 大库门禁2项通过（`/tmp/XunJian-0.1.8-scale-accepted.log`）；Analyze、Runtime、XML/Plist、diff-check通过。
- [ ] 最终三架构签名和公证：首轮Universal因Sparkle辅助程序签名失败，补逐层签名脚本后生成新包，不使用失败包。
- [ ] 提交推送、发布三架构DMG及appcast，在线验证。

截图来自隔离原生测试宿主，OAuth/AI调用0；未修改用户文件。证据：`/tmp/XunJian-0.1.8-tests-final.log`、`/tmp/XunJian-0.1.8-poll-fix.log`、`/tmp/XunJian-release-screens.log`。发现历史公证profile为`XunJian-Notary`，仅使用Keychain引用，不读取凭据正文。未验证项仍保留人工验收边界。

## 已完成：9M 窄窗统计与查重验收

用户确认修复上一轮三项：窄窗数字截断、千组查重交互验证、隔离清理恢复验收。保持现有界面和业务规则，不提交发布、不操作真实文件。
- [x] 统计只显示数字，保留完整宽度；横向不足自动纵排，复用统计组件做大数截图。
- [x] 清理服务最小依赖注入；临时目录运行真实协调器、SQLite、文件移动与撤销，模拟第二个文件失败。补齐有效授权书签；同时刷新索引后磁盘与索引恢复一致。
- [x] 千组滚动区域（约49074pt）与五次位置切换检查、大数窄窗截图、相关回归通过。
- [x] 原生展开/收起验收：通过真实SwiftUI节点的accessibilityPerformPress操作，断言0→1→0；五次滚动位置切换后再次断言0→1→0。展开截图包含三条文件及操作按钮。使用AnyObject调用公开ObjC方法，避免NSAccessibilityProtocol转换漏掉SwiftUI节点；等待实际状态更新，不重复点击掩盖失败。

最终证据：`/tmp/XunJian-9M-accepted.log`66项0失败、退出0，包括51项业务回归、14项页面结构和1项原生千组验收。截图目录`/var/folders/7k/l9rwl1n113x1vty93kzlnjlr0000gn/T/XunJian.DuplicateLayout.A92D2160-25C8-4D38-9B91-E16B3192E653`，主Agent查看duplicates-expanded.png确认三条文件与操作；360pt九位数字截图前轮已验收。滚动循环0.553秒（含5次60ms等待），仅为长阻塞门禁，不代表FPS。调用真实无障碍操作，不宣称外部鼠标事件验收。未清理用户文件、未运行真实OAuth/AI；隔离清理测试在临时目录模拟废纸篓。普通宿主窗口曾触发既有自动索引，已退出；临时改标识宿主移至废纸篓，临时xctestrun已删除。9M三项验收完成，未提交发布。

## 已完成：9L 审查六项修复

用户授权修复存储刷新、清理任务状态、启动误报空库、大量查重结果展示、扫描入口与设置密度。范围为StorageInsightsView、AllFilesView、SettingsView及相关回归；保留设计、搜索与扫描规则，不操作真实文件清理，不提交发布。
- [x] 增加生命周期回归，旧实现1项测试6处断言失败（`/tmp/XunJian-9L-red.log`）。
- [x] 存储统计独立刷新，保留查重结果并标记过期，清理标记不随索引清空；查重组惰性展开。空列表等待当前展示快照。设置精简顶部、提高说明可读性、分开搜索内容和扫描范围，隐藏非当前范围入口。
- [x] 编译、相关回归、明暗宽窄设置截图及实机检查；保留未测边界。

验收：`9L-green.log`14项通过（含设置截图），输出16张780/1440明暗四分区截图，目录`/var/folders/7k/l9rwl1n113x1vty93kzlnjlr0000gn/T/XunJian.SettingsAlignment.6B4B5AB5-3911-4929-ABA1-F6E420509EA0`；抽查通用/文件/AI代表图。最终`/tmp/XunJian-9L-complete.log`14项0失败退出0，含启动展示状态语义回归与存储刷新结构回归。实机启动显示索引准备、随后25987文件；整台Mac设置无“已授权目录0”与重复扫描入口，存储概览打开关闭正常。未执行真实查重/清理、OAuth/AI及海量结果性能压测；清理并发状态由源码回归覆盖，不宣称真实破坏性操作端到端通过。新观察：存储概览窄窗总数仍有省略，记录待后续范围处理。未提交发布。

## 已完成：9K 资料集侧栏选中样式

用户确认去掉重复背景、采用原生侧栏选中样式，收紧行高和图标，对齐边距，标题简化为“资料集”。仅调整SidebarView显示，保留导航、保存搜索和拖入归类逻辑；不提交发布。
- [x] 去掉资料集行自定义选中背景、前景、动画和重复内距，使用sidebar列表及Label。
- [x] 编译及相关检查，实机检查点击/键盘切换、选中对比和行间对齐。

证据：`/tmp/XunJian-9K-sidebar.log`编译及12项SupportingPageStructureTests通过、退出0；CUA重启最新Debug，点击“工作”确认只有原生蓝色圆角选中背景、白色图文，紧凑行对齐；Down切换到“项目”，AX选中同步。保留dropDestination与原有导航绑定，未拖入真实文件。未提交发布。

## 已完成：9J 界面文案

用户授权继续打磨并去掉文案中的宣传腔。范围为主要页面、空态、操作提示及AI确认说明；中英文同步，保留真实功能名称、用量和文件发送边界，不改业务逻辑。保留9I居中设置及既有改动，不提交发布。
- [x] 检查搜索、最近、设置、资料集、阅读栏、命令面板、存储及AI弹窗；修改空泛、重复或工程化的措辞，不为改而改。
- [x] 编译、相关结构检查及原生设置/弹窗截图，核对换行与长度。

证据：`/tmp/XunJian-9J-copy.log` 编译及12项 SupportingPageStructureTests 通过，退出0；重新打开最新Debug，CUA确认通用、文件与索引页面及AI搜索弹窗新文案可见、无截断，保留设置对齐。AI搜索弹窗仅打开后取消，未发送请求、修改设置或主动扫描。中英文源码同步，英文全页面截图及真实OAuth/AI未测；未提交发布。

## 已完成：9I 跨页面细节打磨

保留已认可的黑白编辑台；本轮修正筛选空态、窄阅读栏拥挤、设置内容对齐、图标命中区域及资料集术语。先复现再修改，使用隔离原生夹具与相关回归验收；不改变搜索算法、个人文件或认证，不提交或发布。
- [x] 空态区分索引不可用、筛选无结果和关键词无结果；启动及重试等待首批快照发布再宣布就绪。
- [x] 阅读栏标题与操作按可用宽度布局，筛选/保存命中区域放入标签，资料集入口文案统一。设置初次修正仍偏左，经用户指出后改为整块760pt内容列居中、28pt内距对齐原生表单。
- [x] 编译、相关回归、明暗宽窄截图及安全实机检查完成。

诊断：原生结果列随阅读栏收窄的现有回归已通过（`/tmp/XunJian-9I-investigate.log`，1项0失败），不据截图猜测修改列表尺寸逻辑。

证据：CUA复现有2.6万文件但大小筛选空态误称需添加位置；修正后组合筛选清除保留关键词并恢复11条结果。`9I-startup-red.log`首次快照测试产生1项预期断言失败；`9I-final.log`99项0失败、退出0；最终重试补充和设置修正的`9I-settings.log`2项0失败、退出0，含四分区16张原生截图（780/1440pt，明暗），已逐分区检查代表图。`9I-layout.log`2项0失败、退出0，阅读栏280/420/620pt及其他主要页面布局已捕获。截图目录见各日志。最终Debug停在设置，验收查询/筛选已清除；应用自行执行既有启动补扫，未主动扫描、操作原文件或请求OAuth/AI。PDF夹具存在空白渲染、系统OCR及临时SplitView约束警告，未据此宣称PDF正文、跨系统、完整VoiceOver或长期性能全部通过。主Agent自检，未委派审查，未提交发布。

## 已完成：9H 审查后收敛

已批准保留黑白编辑台，合并搜索工具区，移除类型与计数重复；保存搜索独立命名表单；资料集缩紧行高与内容宽度；设置临时隐藏资源目录并保留返回后的偏好。不提交、推送、发布或修改用户文件。
- [x] 行为回归：设置切换不覆盖目录偏好，保存搜索条件与取消保持正确。
- [x] 完成搜索、资料集、设置原生布局调整。
- [x] 编译、相关测试、明暗宽窄布局与安全实机操作验收。

证据：`/tmp/XunJian-9H-red.log`新增目录策略测试在接口实现前编译失败；`/tmp/XunJian-9H-final.log`124项0失败、退出0（包括隔离原生布局）。随后统一按钮样式并把系统目录按钮移除规则移到所属侧栏，最终`/tmp/XunJian-9H-build2.log`编译退出0。CUA确认最终设置页目录/开关消失、返回恢复展开偏好；保存表单空名禁用、输入启用、Escape取消不改查询；100MB筛选清除保留文档类型与查询；780pt窗口资料集8行完整显示。截图绝对路径由final日志的`THEME_SNAPSHOT_DIRECTORY`记录。未改变个人文件、未主动扫描/请求AI；跨macOS和长时性能未验证。Figma额度仍耗尽，未继续写入。9G及更早改动完整保留。


## 已完成：9G 保留编辑台的专项精修

用户认可9F，授权继续打磨重复入口、交互、逻辑、性能与动画；不更换视觉方向，不提交/发布。
顺序：核对主要页面与热路径 → 修复已确认问题 → 原生回归与实机操作 → 更新交接。
- [x] 设置仅保留窗口工具栏及系统菜单/快捷键入口，目录回归资源功能。
- [x] 顶部导航各自可识别；目录键盘只操作可见条目，不跳到已移走的页面。
- [x] 文本预览复用未变化的匹配与富文本内容，切换匹配只导航；内容/主题变化及时更新。
- [x] 命令面板动效仅作用于浮层，保留减少动态效果、焦点和取消行为。
- [x] 检查搜索/筛选入口/资料集/最近/设置/阅读相关路径，完成专项测试与新版实机验证。
验收：重复设置消失，键鼠导航一致，富文本无变化时不重复构造，样式与内容无陈旧缓存；大库长期性能与不同macOS版本无实时证据时明确保留边界。继续保留8V–9F累积改动。

证据：`/tmp/XunJian-9G-red.log` 3项预期失败；首轮118项通过；最终 `/tmp/XunJian-9G-final.log` 175项0失败、退出0，覆盖原生文本切换、列表选择及明暗/宽窄布局。独立代码审查 SCOPED APPROVED。CUA核对目录上下键、唯一设置入口、三导航标识、设置四分区、最近/资料集空态、新建取消、搜索清除、筛选弹窗及⌘K/Escape；数字筛选输入和保存搜索混排未完成实机验收。详情见 `.impeccable/review/9g-verification.md`。未做大库长时、跨系统、完整VoiceOver及FPS测量，不代表全软件零缺陷。

## 当前执行：9F 黑白编辑台

用户已确认构图并要求做出可运行版本。以 docs/DESIGN-9F.md 和已批准 mock 为准。
- [x] 原生顶部导航、通栏搜索与宽阅读区；取消 9E 轨道/页面套框，已捕获最终明暗实机效果。
- [x] 结果、资料集、最近、设置和共享控件采用平面层级；保留既有操作实现。
- [x] 修复网格键盘列数边界；验证搜索焦点、阅读开关、窗口缩放和明暗布局。
- [x] 独立设计复核并修复真实PDF页码，打开独立Debug供试用；不发布。
- [x] 从当前原生实现记录DESIGN.md和.impeccable/design.json，结构及引用校验通过。
- [ ] 设计流程全项关闭：concept-roll历史记录缺失，已披露但独立复核仍为partial，不补造seed。

当前：Mac已解锁，最终浅/深/窄窗/设置/资料集截图和CUA键盘、阅读开关、缩放操作已核对。169项相关行为检查通过；旧窗口约束循环修复后布局复验通过，页码19项专项通过，最终9F-light-confirm.log布局1项97.250秒0失败、退出0。独立设计verdict为fix：页码resolved，缺失concept-roll记录partial；不将流程披露冒充合规通过。证据见.impeccable/review/9f-verification.md；最近、弹窗、动效及全量业务不作本轮完整实机验收声明。

## 发布状态

- [x] 0.1.7（build 8）已完成设置草稿、正文预览命令、全窗口细滚动条与 OAuth 最终体验修复；真实 Codex/Grok OAuth 与模型列表验收通过，未发送模型请求。主工程/OAuth Process 共 463 项、5 万/10 万门禁、Analyze 与静态门禁已通过；三架构 Developer ID 签名、公证、GitHub Release 与 Sparkle appcast 均已完成。
- [x] 0.1.6（build 7）已完成菜单栏后台偏好通知崩溃、SQLite 启动阻塞/状态误报与全局滚动条回退修复；主工程 406 项（2 项大型门禁跳过）、OAuth Process 38 项及 5 万/10 万门禁均 0 失败。修复版三架构 Developer ID DMG、Apple 公证、staple、Gatekeeper、挂载复验、GitHub Release、Sparkle EdDSA 与线上 digest/size/下载均已完成。
- [x] 0.1.4（build 5）已完成索引迁移性能修复、三架构签名/公证/门禁、GitHub Release、Sparkle EdDSA 与 HTTPS 验收。
- [x] 0.1.5（build 6）已完成扫描/监控/排除、原子正文索引、OAuth 模型恢复、UTF-8 Prompt 门禁、大结果集全选/导出、确认框与无障碍修复；主工程 401 项（2 项大型门禁跳过）、OAuth 进程 38 项及 5 万/10 万门禁均 0 失败。三架构已完成 Developer ID、公证、staple/validate、Gatekeeper、挂载签名/架构、官方 Runtime、GitHub Release、Sparkle EdDSA 与 HTTPS 验收。

## 产品目标

以原生 macOS SwiftUI 应用统一展示用户主动授权目录中的文件，通过本地索引、快速搜索与最小必要的 AI 能力帮助用户找到、理解和简单管理文件。默认不复制、不移动、不改变原目录结构。

## 固定边界

- Local First；真实删除、移动、重命名只能由用户主动触发。
- AI 只接收完成当前请求所需的最少内容，不自动执行文件操作。
- 不做云盘、Finder 替代品、知识库、RAG 平台、Agent、协作或自动整理工作流。
- 原生 SwiftUI、Foundation、AppKit 与系统框架优先；只引入必要的轻量依赖。

## 实施阶段

- [x] Phase 1：基础工程、Sidebar、首页、所有文件、分类、设置、统一搜索入口与基础视觉。
- [x] Phase 2：目录授权、Security-Scoped Bookmark、文件扫描、Metadata、SQLite 与文件列表。
- [x] Phase 3：Quick Look、缩略图、打开、Finder 定位、安全文件操作与多分类。
- [x] Phase 4：文本提取、SQLite FTS5、本地搜索、过滤与排序。
- [x] Phase 5：FSEvents 增量监听、文件身份识别与索引一致性。
- [x] Phase 6：本地 API Key 凭据文件、统一 AI Provider、DeepSeek、Qwen，以及按官方能力评估 Codex/Grok；完成四项 AI 能力。
- [x] Phase 7：错误与空状态、性能、权限恢复、网络异常、UI 交互与完整验收。
- [x] Phase 8L：P1/P2/P3 交互原生化；搜索、列表/表格、展开区、信息分组、选择器、进度与操作组交还系统控件，业务与安全边界保持不变。
- [x] Phase 8M：深度审查收尾；扫描数据安全、十万文件主线程卡顿、UI/无障碍及 OAuth/XPC 并发与签名权限边界已关闭。

## Phase 2 验收

- 系统目录选择器真实授权，Bookmark 可在重启后恢复。
- 扫描不阻塞主界面，支持取消、进度反馈与默认目录排除。
- SQLite 持久化扫描源和文件 Metadata，数据库目录 0700、文件 0600。
- 首页、文件表格/网格、基础名称/路径筛选和 Inspector 使用真实索引数据。
- 空目录、嵌套目录、中文、Emoji、隐藏文件、长文件名、1000 个文件与数据库替换均有测试。

## Phase 3 验收

- 列表、网格与 Inspector 展示系统缩略图；支持打开、Quick Look 与 Finder 定位。
- 重命名和移动执行真实文件操作，拒绝非法名称、目标冲突和不可写位置。
- 移到废纸篓必须二次确认，并明确可从系统废纸篓恢复。
- 分类支持新建、重命名、删除和多分类关联；删除分类不删除真实文件。
- 分类及关联写入 SQLite；重新扫描和退出重启后仍可恢复。

## Phase 4 验收

- 支持常见纯文本、Markdown、JSON/YAML/XML/CSV/HTML、源码与可直接提取文本的 PDF。
- SQLite FTS5 索引文件名、路径、分类与正文；中文查询、特殊字符和索引刷新均安全可用。
- 搜索异步执行并带 120ms 防抖，不调用 AI；支持文件类型过滤。
- 支持相关度、名称、修改/创建时间、大小、类型排序及升降序切换。

## Phase 5 验收

- FSEvents 仅监听可用的已授权来源，按文件事件识别新增、删除、修改、重命名与移动。
- 350ms 合并同源事件，只扫描受影响文件或目录，并在单个 SQLite 事务内同步 Metadata、分类与 FTS。
- 文件 ID 结合卷与资源标识；同一来源内重命名/移动保持身份和分类关联。
- 删除/移出目录执行轻量路径存在性校验；事件丢失、根变化时才回退一次完整扫描。
- 应用启动直接恢复持久化索引，不自动重新完整扫描。

## Phase 6 验收

- DeepSeek、Qwen、Codex、Grok 复用统一 Chat Completions 边界；Base URL/Model 可配置，一次只有一个当前 AI。
- API Key 仅存当前用户的 `Application Support/XunJian/Credentials/ai-credentials.plist`；目录 0700、文件 0600，缺失时自动重建为空文件。UserDefaults 只保存 Base URL、Model 与当前 Provider。
- API Key 不写入 App 包、索引或日志，分享或复制 App 不会携带密钥；本地文件不触发钥匙串密码提示，但不具备 Keychain 的系统级加密保护。
- 顶部搜索始终执行 120ms 防抖的纯本地索引搜索；AI 搜索只由用户点击独立入口后触发。
- AI 看文件、问文件不上传路径或无关文件；正文单次最多 4 万字符。AI 分类最多 8 个文件，只建议已有分类，确认后才应用。
- Phase 6 聚焦测试 13/13 通过，覆盖本地凭据持久化、缺失重建、权限、App 包隔离、Provider 独立删除、符号链接拒绝、请求/响应与 AI 能力。

## Phase 7A 验收

- 设置页支持新增目录、失效目录重新授权，以及二次确认后移除已授权目录。
- 移除来源只删除 Bookmark 与本地索引；原文件不变，FTS、文件和分类关联无孤儿记录。
- 外观支持跟随系统、浅色与深色，选择持久保存并即时作用于整个窗口。
- “所有文件”顶部仅保留 AI、类型、排序、升降序和显示方式控件；所有控件保持单行，文字标签隐藏但无障碍名称保留。
- 窗口变窄时，靠右控件按显示方式、排序方向、排序、文件类型的顺序进入“更多”；菜单内仍可完成全部操作。
- 低于 1080pt 自动收起 Inspector，低于 960pt 自动收起 Sidebar，并用滞回阈值恢复自动收起的栏；手动收起状态不被强制恢复。
- 窗口最小宽度为 360pt、默认 1200pt；控件显隐同时参考实际内容宽度，双侧栏最大宽度时也不挤压。
- 六列在实际内容宽度 1020→640pt 间连续压缩；低于 640pt 后所有列保持存在，由右边界逐像素裁切，放宽时对称恢复。
- 未授权目录且没有文件时，首页提示和授权入口在可用内容区域内居中，窄窗下仍完整显示。
- 现有 32 项测试、独立签名构建、真实 UI 与数据库完整性验收通过。

## Phase 7B 计划

### 7B-1 表格密度与访达日期（已完成）

- 为六列补齐合理的 `min / ideal / max` 宽度；实际内容区在 1020→640pt 间连续收缩，达到可读下限后从位置列一侧逐像素遮挡。
- 所有列始终保持可见状态；表格使用最小 640pt 内部画布与真实内容宽度外层裁切，不重建或改写 Table customization。
- 修改时间采用系统短日期、短时间与相对日期格式；中文显示“今天 01:49 / 前天 15:34 / 2026/8/2 02:09”，英文按所选语言区域显示。
- 验收宽屏、每个断点上下 1pt、双侧栏收放、今天/昨天/前天/普通日期及空日期。

### 7B-2 中英文界面

- [x] 第一部分：增加跟随系统、简体中文、English 三种持久化语言状态和根 Locale；接入 String Catalog，完成首页、Sidebar、“所有文件”及其 AI 弹窗、数量、类型、排序与 Finder 日期。语言开关暂不展示。
- [x] 第二部分：补齐设置、分类、Inspector、AppShell 弹窗、运行时错误和系统面板；默认分类只做显示层翻译，不改数据库中的用户数据或用户重命名结果；设置页 Picker 已启用并支持即时切换。
- [x] AI 协议字段、模型指令、文件名、路径及用户分类不随界面语言误改。
- 完整范围约 15–17 个文件，超过单阶段 10 文件边界；拆为“基础设施与主要页面”“弹窗/错误/默认分类”两步，全部覆盖前不暴露半成品语言开关。

### 7B-3 Codex / Grok 登录

- 最终产品必须提供 App 内置、可随 App 直接分发的 Codex/Grok OAuth 登录，不要求用户预装或登录本机 Codex/Grok CLI。
- 现有基于本机 CLI/XPC 的 OAuth 实现仅作为技术原型，已停止真实 smoke；下一阶段需先核实两家官方第三方 OAuth 能力，再以官方支持的嵌入式流程替换，不能复制本机 CLI Token 或模拟网页登录。
- OpenAI / xAI API Key 保留为独立回退；OAuth Token 由官方客户端管理，不写入 UserDefaults、日志或崩溃信息。
- 当前 App Sandbox 无法可靠读取外部 `~/.codex`、`~/.grok` 会话；已确认采用独立签名伴随服务，保留主 App 沙盒。
- 登录验收覆盖开始、取消、失败、成功、重启恢复、过期、断开、明确确认后的全局注销，以及未安装/版本不兼容；只有协议握手和一次真实请求成功后才显示已连接。
- [x] 7B-3A：建立按需嵌入、无 App Sandbox entitlement 的私有 XPC 服务、版本化 IPC、双向签名验证与官方签名 CLI 探测。
- [x] 7B-3B：接入 Codex App Server、Grok ACP、进程监督与协议测试。
- [x] 7B-3C-1：升级 v2 认证 IPC，接入 Codex ChatGPT 登录、Grok 官方 CLI OAuth、状态/取消/仅断开及 AppModel 独立 OAuth 状态；不触发真实登录或模型请求。
- [x] 7B-3C-2A：接入设置页、中英文状态、前台轮询和只断开；认证版本精确白名单为 Codex `0.146.0`、Grok `1.0.0 (3cd0d0cbcebe)`，兼容 Grok 当前四类只读被动通知，不执行登录或模型请求。
- [ ] 7B-3C-2B：v3 `verifyConnection`、0600 无工具 Agent Profile、专属 `GROK_HOME`、合成 `HOME`、跨 XPC 租约、`inspect --json` 与 owned UUID 清理已实现；Codex `0.146.0` 继续安全阻断。Grok 专属 OAuth 登录及两次无提示预检已通过。离线已补固定静态阶段码、精确中英文白名单及有序完整 post-prompt 生命周期，缺失/乱序/重复/ID 漂移全部 fail closed；Protocol 50/50、OAuthBridge 38 项（真实 smoke 1 项跳过），合计 88 项、0 失败。本阶段未发真实模型请求；下一步经用户再次确认后执行一次真实 smoke，再验收刷新、重启、过期与仅断开。
- 伴随服务只接受固定操作与固定官方 CLI，按 OpenAI Team `2DC432GLL2`、xAI Team `5Y6N3AJ54S` 校验签名；不经 shell、不继承 API Key/DYLD 环境、不读取或返回凭据文件。仅断开寻简不注销共享 CLI，会影响全局会话的 logout 必须再次确认。

#### 7B-3D 私有 Runtime 下载原型（已停止继续扩展）

- 推荐采用“寻简管理的私有官方运行时”：用户点击登录时由寻简自动下载固定版本的 Codex App Server / Grok Build 到自身 Application Support；不扫描 PATH、不复用 `~/.codex`/`~/.grok`，用户无需安装 CLI。
- 仅从 OpenAI/xAI 官方发布地址下载；下载后依次校验固定版本、SHA-256、Developer ID Team/identifier、非链接与权限，再原子安装。启动始终使用绝对私有路径，不经 shell。
- [x] 7B-3D-1（5 文件）：私有 Runtime Store、官方清单、流式硬限额、独立取消、release 指纹、SHA-256/Developer ID、raw/tar 受控解包、进程组清理、单飞/跨 Store 原子安装；固定 Codex App Server `0.147.0`、Grok `1.0.0` arm64，Process 38/38 通过，未登录或发模型请求。
- [x] 7B-3D-2A（10 文件）：Codex 浏览器 OAuth 使用私有 App Server `account/login/start(type: chatgpt)`；首次登录自动安装，状态刷新离线识别，专属 `CODEX_HOME`、合成 `HOME`、跨 XPC 租约、取消/仅断开与浏览器跳转均不依赖本机 Codex；Process 40/40、Protocol 50/50、Bridge 37 通过 + 1 个真实 smoke 跳过，0 失败；未执行真实登录或模型请求。
- [x] 7B-3D-2B（10 文件）：`chatgptDeviceCode` 的 v4 协议/UI、attemptID 绑定与 fail-closed 校验完成；真实设备码及浏览器 OAuth、重启恢复、刷新、“仅断开”与再次恢复均通过。固定 App Server `0.147.0` 正确 stdio 参数，并禁用 bundled skills/plugins、兼容其受限官方骨架。离线 Bridge 43 项（真实 smoke 1 项跳过）、Protocol 53/53 与本轮兼容窄测 3/3 均 0 失败；未发送模型请求。
- [x] 7B-3D-3（8 文件）：Grok OAuth/ACP 生产路径已切换至寻简私有 Managed Runtime；`probe/status` 离线校验，首次登录才按需下载，登录/验证/Session 删除均复验同一私有路径；复用专属 `GROK_HOME`、状态/取消/租约与清理，不依赖或回退本机 Grok。定向测试 3/3、Swift 6 strict typecheck 与 Debug App/XPC build 均通过；未执行真实下载、OAuth 或模型请求。
- [ ] 7B-3D-4：离线完成双架构 App/XPC、许可 NOTICE、升级与损坏恢复回归，但“首次登录下载 Runtime”不符合最终产品要求，停止继续扩展；不得把该原型标为可用 OAuth。
- 该方案面向 Developer ID 签名与公证后的站外 DMG/ZIP 分发；若目标是 Mac App Store，不能在运行时下载可执行代码，需改为把运行时直接打进 App，体积和审核风险单独评估。

#### 7B-3E 可用 OAuth 重建（内置分发方案已确认）

- 最终边界：只发布公证 DMG；Codex App Server 与 Grok Runtime 随 DMG 内的应用分发，不扫描或调用用户本机 CLI，不在登录时下载可执行代码；每位用户的 OAuth 凭据只写入自己的 Application Support。API Key 继续使用现有本地 0600 文件作为独立回退。
- [x] 7B-3E-1A（9 文件）：OAuth Bridge 协议升至 v5，新增 provider 绑定的 `generateText`；输入按 model/system/user 分段与总量门禁，输出上限 128 KiB，服务 75 秒、客户端 100 秒超时。Codex 使用固定 0.147 restricted-read、ephemeral thread、唯一 final + completed 聚合；Grok 复用严格 owned session 生命周期并返回有界通用文本。成功/失败/取消均关闭进程并清理临时目录，Grok 另删除精确 Session。App/XPC build 成功，OAuthBridge 46/46、OAuthProtocol 58/58，共 104/104 通过；未启动真实 CLI、OAuth、网络或模型。
- [x] 7B-3E-1B（6 文件）：新增 OAuth `AIProvider` 与持久化认证模式，让 `.signedInUnverified/.connected` 可独立设为当前 AI；搜索、解释、问答与分类通过现有 `AIService` 走 OAuth Chat IPC，API Key 本地凭据与 Provider 路径保持独立。OAuth 失效只清除 OAuth 当前项；设置页显示/切换当前模式。新增聚焦 5/5、OAuthBridge 51/51、Swift 语法检查与 Debug App/XPC build 均通过；未启动真实 CLI、OAuth、网络或模型。
- [x] 7B-3E-2A（10 文件）：Codex App Server 0.147.0 的 arm64/x86_64 官方签名产物已随 XPC Bundle 打包；状态、登录和 AI 请求只解析当前架构资源，并复验固定 SHA256、OpenAI Team/Identifier、权限与链接，Codex 登录下载路径已移除。Resolver/Bundle 4/4、Swift 6 strict typecheck、Debug App/XPC build 与产物签名/SHA 均通过。Release 已使用 Team `76V7CQ4T45` 的 Developer ID 签名并经 Apple 公证接受、stapling 与 Gatekeeper 验证；可分享 ZIP 位于 `Release/寻简-0.1.0-macOS.zip`。未启动 Runtime、OAuth、网络或模型。
- [x] 7B-3E-2B（10 文件）：Grok Build 1.0.0 的 arm64/x86_64 官方签名产物已随 XPC Bundle 打包；状态、登录、验证、AI 请求与 Session 删除只解析当前架构资源，并复验固定 SHA256、xAI Team/Identifier、权限与链接，Grok 登录下载路径已移除。App/XPC Release 均为 `arm64+x86_64`；Resolver/Bundle 3/3、Release Universal build、资源 SHA 与供应商签名检查均通过。未启动 Runtime、OAuth、网络或模型；2A 的旧 ZIP 尚未包含本阶段产物，不作为最终分发包。
- [x] 7B-3E-3：删除不可达下载器/旧 smoke/误导文案；Codex、Grok 生产路径与离线回归已闭环。0.1.2（build 3）及 0.1.4（build 5）三架构均完成 Developer ID、公证、staple/validate、Gatekeeper、挂载签名/架构、官方 Runtime、GitHub Release 与 Sparkle HTTPS 验收。

### 7B-4 API Key 本地持久化（已完成）

- Keychain 已替换为当前用户 Application Support 下的寻简本地凭据文件；App 分享不携带密钥，文件缺失自动重建。
- 保存、读取、删除、Provider 创建和设置文案已完成切换；旧 Keychain 条目保持原样且不再读取。

### 7C 大规模索引性能门禁（已完成）

- 新增只在发布验收时显式启用的 5 万文件性能门禁，覆盖批量写入、全量恢复、FTS 唯一命中、数据库体积和结果正确性。
- arm64 实测 50,000 条完整门禁 1/1 通过，XCTest 总用时 0.790 秒；日常测试默认跳过，避免拖慢普通回归。

### 7D 10 万文件与列表交互门禁（已完成）

- 新增 100,000 条发布验收，覆盖索引写入/恢复/FTS、类型筛选和名称、修改时间、创建时间、大小、类型五种排序；每项交互限时 1 秒。
- 门禁发现类型排序 1.099 秒的回归；将七种固定类型的本地化比较结果缓存为序位后降至 0.701 秒，排序语义不变。
- arm64 最终实测：写入 1.192 秒、恢复 0.107 秒、FTS 0.0013 秒、筛选+五种排序 0.998 秒；门禁与原排序正确性测试 2/2 通过。

### 7E 权限失效与重新授权验收（已完成）

- Bookmark 可在授权目录移动后解析到新位置；损坏 Bookmark 明确进入需重新授权状态。
- 重新授权不再强制目录仍处于旧路径，用户可选择移动后的当前位置；来源 ID、文件身份、分类与 FTS 索引保持一致。
- 移除来源会事务性清除来源、文件、FTS 与文件分类关联，不删除用户磁盘文件，也不删除可复用的分类实体。
- 聚焦验收 4/4 通过；未改 OAuth、公证包或性能实现。

### 7F UI 与命中区域完善（已完成）

- 首页、分类和文件网格增加轻量 hover/选中反馈；最近文件使用真实缩略图与统一访达式日期。
- 文件表格六列、分类文件行、网格卡片和 AI 分类行均支持整行单击、双击打开与右键菜单；图标按钮统一扩大命中区。
- OAuth 官方账号卡片按状态、说明、操作分层；Codex/Grok 均提供显式“验证连接”，只有固定最小真实请求与完整清理成功后才转绿。
- 寻简专属 OAuth 新增明确确认的“退出账号”；已移除共享 CLI 原型遗留的“仅断开寻简”入口与文案。
- 授权目录、AI 字段、分类标题、首页日期、扫描状态与弹窗在 360pt/英文环境下自动换行；Inspector 与网格状态层级保持一致。
- 顶部搜索恢复为纯本地索引搜索，不再因输入自然语言自动调用 AI；AI 能力保留独立显式入口。
- 离线 OAuthBridge 54/54、OAuthProtocolClient 60/60 与 Debug 构建均通过；未触发真实 OAuth、网络或计费模型请求。

### 7G 操作逻辑与恢复路径（已完成）

- AI 正文只在明确请求时按文件 ID 读取；英文界面使用英文指令，关闭任务弹窗会取消请求并阻止迟到结果回写。
- AI 搜索条件保留，普通搜索作为本地二次筛选；AI 分类支持本地筛选、已选置顶、逐条移除建议、事务性应用与一次撤销。
- 扫描用 generation、当前来源和待扫描集合隔离旧任务；移除来源不会让旧任务覆盖新状态。
- 删除分类自动回到分类总览；筛选隐藏文件清理选择，移动/重命名成功后按新路径恢复选中。
- API Key 保存以成功结果驱动清空与提示；Base URL/模型变化后必须先保存再重新验证，旧绿态不再误导。
- 数据库不可用持续显示恢复提示并禁用索引操作；无分类、授权失效及类型空结果均提供明确恢复路径。
- Debug 构建通过；FileIndex/Navigation/AI 聚焦 30 项执行、28 通过、2 项发布级大数据门禁按设计跳过，0 失败；未触发真实 OAuth、网络或计费请求。
- 当前待公证 DMG 早于本阶段源码；正式交付 7G 前必须重新构建、签名并公证新 DMG，不得把旧产物标为包含本轮修复。

### 7H 文件浏览偏好与应用图标（已完成）

- 扫描默认忽略名称以 `.` 开头的文件与目录；设置可显式开启，切换后立即更新可见结果并重扫已授权来源。
- 所有文件表格取消双击优先造成的单击等待，单击立即选择、双击仍打开。
- 文件类型、排序、升降序与列表/图标显示方式通过用户偏好持久化，切换页面和重启后保留。
- 新增完整 macOS AppIcon asset catalog；隐藏文件扫描回归 4/4 与 arm64 Debug 构建通过。

### 7I 最终数据一致性与恢复体验（已完成）

- 搜索返回总数并支持 500 项分批加载；普通搜索保留旧结果直到新结果提交，浏览/搜索排序与方向、表格列布局及列表/网格滚动位置独立记忆。

### 7J 全面审查报告闭环（已完成）

- [x] F01–F24 全部完成：动效/Reduce Motion、Dynamic Type、640pt 完整表格、多选拖拽快捷键、职责拆分、缓存/索引性能、设计 Token、无障碍/本地化、失败反馈、Provider 合并、SSE 流式与默认分类单一来源。
- [x] 报告 21 项增强全部实现：复制路径、Quick Look、批量分类、手动过滤、搜索历史、Inspector AI、分类排序、来源启停、保存搜索、命令面板、内嵌预览、拖入拖出、结果导出、菜单栏搜索、Services、存储洞察、通用撤销、Finder 标签、自定义列、扫描通知、重复检测。
- [x] 复审 P1/P2/P3：扫描完整性、撤销身份、原生文本命令、Services 类型、AI revision、FSEvents 测试、版本、完整哈希、CSV 公式注入、缩略图等待取消与 Swift 警告均闭环。
- [x] arm64 Debug 通过，离线回归已闭环：App 237 项全量执行（2 项发布级大数据门禁跳过）；初次暴露的 FSEvents 路径规范化与 OAuth 英文文案失败已修复并定向 2/2 复跑通过。OAuth Process 34/34。
- [ ] 发布前人工验收：拖拽、Services、菜单栏、VoiceOver、放大字号/Reduce Motion、常见窗口宽度；确认后再构建和公证新 DMG。
- 全量扫描的枚举或元数据读取失败会整批拒绝替换；增量扫描只对成功 scope 做差集更新，失败 scope 不再触发全源清理，人工分类不会因临时权限或 iCloud 错误丢失。
- 数据库运行时失败会停止监控、释放旧实例并由“重试”重新打开；恢复性全量扫描排队且不取消用户扫描，过期文件会清理失效索引。
- 授权来源拒绝相等、父子或符号链接重叠；跨授权来源移动通过单事务迁移分类、索引和 FTS，失败不主动破坏旧分类关系。
- 单主窗口消除跨窗口搜索/选择/危险弹窗串扰；OAuth 轮询提升到 App 生命周期，API Key 测试单飞可停止，凭据损坏与快速分类连点均有安全恢复。
- App 测试 232 项执行（230 通过、2 项发布门禁跳过）、Process 34/34、arm64 Debug 构建通过；本阶段未生成、签名、提交或公证新 DMG。

### 7K 最终并发、性能与窗口语义收尾（已完成）

- [x] 正文索引开关串行化；正文提取按有界批次流式写库；Security Scope 失败进入重新授权。
- [x] 破坏性操作在同一 actor 内复验身份并执行；FSEvents 对存在/已移走路径使用一致的系统路径别名。
- [x] 导出后台流式原子写入，文本预览匹配后台防抖，存储洞察用有界 Top 10。
- [x] 页面命令目标不再串页；保存中弹窗不可误关；错误由关键窗口独占；日期过滤提供明确开关与日期选择。
- [x] 最终离线回归：App 246/246、OAuth Process 34/34；5 万文件门禁 0.860 秒、10 万文件门禁 2.797 秒。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

### 7L 卡顿、扫描与搜索一致性最终收尾（已完成）

- [x] Runtime 哈希缓存补 ctime 与前后身份复验；Codex 固定精确模型；超时子进程最终回收。
- [x] 搜索保留旧结果、稳定分页并随索引变更重查；派生索引、大排序和分类筛选移出主线程。
- [x] 扫描启停/权限状态统一，增量任务串行；失败 scope、隐藏重命名、Finder 标签及正文提取消正确处理。
- [x] 分类状态改为按文件增量维护；批量分类与批量移除合并提交，AI 分类具取消和 revision 门禁。
- [x] 最终离线回归：arm64 Debug 通过；App 258 项执行（2 项发布级门禁跳过）、OAuth Process 35/35，0 失败；5 万/10 万门禁分别 1.357/3.165 秒。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

### 7M 文件浏览与派生索引性能收尾（已完成）

- [x] 分类派生索引只在后台计算一次，`CategoryIndexStore` 直接接收结果；批量分类单次修补、单次 revision。
- [x] 删除/遗失文件在后台重建文件与分类派生索引，避免主线程全量过滤和重算。
- [x] 所有文件页的异步快照绑定页面可见性与输入签名，旧任务不能覆盖新筛选或清除其他页面选择。
- [x] 命令目标用快照签名常数时间去重；AI 分类按 ID 查找最多 8 个文件，不再扫描整个索引。
- [x] 缩略图缓存与任务绑定内容版本、尺寸和显示比例；重复选择排序字段保留用户方向。
- [x] 完整扫描保持 fail-closed，移除不可达的“跳过位置后仍成功”状态与文案。
- [x] arm64 Debug 通过；App 260 项执行（258 通过、2 项发布门禁跳过）；聚焦 18/18；显式 10 万文件门禁 3.112 秒。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

### 7N 深层一致性与安全收尾（已完成）

- [x] 数据库重开、全量重载与搜索使用代际门禁；旧 actor、旧查询和旧进度不能覆盖新状态。
- [x] 废纸篓及跨来源移动撤销保留分类；分类删除从数据库原子捕获隐藏成员，Undo 只在写入成功后登记并防并发重复执行。
- [x] 重复文件清理确认后重新哈希；文件操作通过 `NSFileCoordinator` 协调并在移动前后复验身份。
- [x] SQLite 索引目录/文件拒绝符号链接、硬链接、非普通文件及非当前用户所有权。
- [x] 菜单栏搜索与命令面板随文件/分类 revision 刷新；导航命令使用稳定 ID，菜单“打开”支持所有文件类型。
- [x] arm64 Debug 通过；App 265 项执行（263 通过、2 项发布门禁跳过），0 失败。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

### 7O 数据保留、删除竞态与 AI 资源边界（已完成）

- [x] 隐藏文件策略保留未扫描的普通隐藏项；用户主动配置的排除名称会清理对应索引、FTS 与分类关联。正文扫描通过临时表分批暂存并按来源原子提交，取消与失败保留上一份完整搜索快照。
- [x] 废纸篓横幅与通用 Undo 解耦；移除来源只失效依赖该来源的撤销项。
- [x] 重复清理对保留项和候选项执行稳定内容指纹，并在系统文件协调器内复验版本后才移动到废纸篓；操作完成前页面不可误关。
- [x] 自定义 AI JSON/SSE/生成文本加入硬上限；流式输出 50ms 合批；PDF 按页有界提取并响应取消；全局错误按 FIFO 去重展示。
- [x] SQLite 文件使用 `O_NOFOLLOW|O_EXCL` 安全创建，打开前后复验设备/inode/所有权/链接数/类型；继续拒绝符号链接与硬链接。
- [x] arm64 Debug 通过；聚焦离线回归 57/57，0 失败。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

### 7P 主线程与 AI 流式完整性收尾（已完成）

- [x] 隐藏文件过滤、AI 结果交集、分类可见 ID 和 AI 搜索后处理移出主线程。
- [x] OpenAI 兼容 SSE 必须收到 `[DONE]`；截断流不再把半截回答当成功，界面同时显示部分内容与错误。
- [x] arm64 Debug 通过；AI 与卡顿隔离聚焦 32/32，0 失败。未运行真实 OAuth、AI 或网络，未生成 DMG。

### 7Q 剩余 P2/P3 性能与交互收尾（已完成）

- [x] 隐藏文件偏好改为单飞；快速切换取消旧整库重载。
- [x] Services 外部路径改用规范路径索引；导出筛选和分类映射移出主线程并保持分类顺序。
- [x] arm64 Debug 通过；导出、卡顿隔离与 AI 聚焦 59/59，0 失败。未运行真实 OAuth、AI 或网络，未生成 DMG。

### 7R 全面审查最终离线验收（已完成）

- [x] 完整常规测试：App 285 项（2 项发布门禁默认跳过）+ OAuth Process 35 项，合计 320 项、0 失败。
- [x] 显式 5 万/10 万门禁 2/2；类型排序由 1.038 秒降至 0.127 秒，10 万总交互 0.643 秒。
- [x] 全源码语法、Swift 6 严格并发、本地化 JSON、diff 与 arm64 Debug 全部通过。
- [ ] 按 `docs/MANUAL_ACCEPTANCE.md` 执行人工体验验收；真实 AI/OAuth 与新 DMG 仍需另行确认。

### 7S 文件浏览、扫描与 OAuth 真实闭环（已完成）

- [x] 文件浏览快照与选择状态减少无关发布和视图重算；扫描正文仅处理新增/变化文件，目录规范路径复用。
- [x] Runtime 生命周期改为 `kqueue` 真实退出监控；忽略 Grok fork 通知，保留退出码并区分长驻协议与有限命令。
- [x] Codex 0.147.0 与 Grok 当前模型生命周期逐字段对齐；轮询、弹窗和失效连接竞态闭环。
- [x] Codex、Grok 最小真实验证均成功，重启后本地证明恢复成功；启动本身不发模型请求。
- [x] 完整离线回归 App 288 项（2 项发布级门禁跳过）+ OAuth Process 37 项，共 325 项、0 失败；arm64 Debug 通过。
- [ ] 继续人工 UI/UX 与长列表滚动验收；按用户要求暂不生成 DMG。

### 7T 深层状态所有权与特殊文件收尾（已完成）

- [x] 跨页面选择、批量废纸篓与异步全选绑定当前命令目标及 generation；隐藏页面不写滚动偏好。
- [x] 大列表 ID 派生移至后台并单次发布；网格键盘复用缓存 ID，日期下限不再包含未知日期。
- [x] 重复检测拒绝链接、FIFO、设备；OAuth 登录启动可取消，断开/退出可安全抢占同 Provider 请求。
- [x] 导出无障碍取消、AI 零分类恢复入口、保存搜索删除确认与 Services 英文补齐。
- [x] App 292 项（2 项发布门禁跳过）+ OAuth Process 37 项、0 失败；显式 10 万文件门禁 2.295 秒；arm64 Debug 通过。未生成 DMG。

### 7U 最终性能发布与 XPC 测试稳定性（已完成）

- [x] 增量索引只在用户可见内容变化时发布；正文异步提取完成后刷新当前搜索，Finder 标签元数据事件精准刷新对应行与 Inspector。
- [x] 分页搜索全选按查询与筛选语义复验，不再被自身的结果 revision 取消；Finder 标签刷新不再清空或重载 Inspector 正文预览。
- [x] 大列表命令目标、网格选择视觉状态和缩略图表示保持稳定；特殊文件与 OAuth Provider 抢占继续 fail closed。
- [x] Xcode 26.6 连续覆盖率会把嵌入式 XPC 卡在 LLVM 初始化的 `open()`；共享测试方案关闭覆盖率采集后，真实 XPC 能力、签名拒绝及协议不兼容测试恢复稳定。
- [x] App 298 项（2 项发布门禁跳过）+ OAuth Process 37 项，合计 335 项、0 失败；OAuthBridge 62/62、OAuthProtocol 64/64；显式 10 万文件门禁 2.094 秒；arm64 Debug 与 Services 英文资源验证通过。未运行真实 OAuth、AI 或外部网络，未生成 DMG。

## Phase 8 正式产品体验升级（实现完成，发布配置与实机验收待完成）

### 8A 文件浏览与 Inspector 性能

- [x] 列表普通单击只走一条选择链路；相同选择不重复发布。
- [x] 网格仅由已实例化卡片观察自身选中状态，不让选择修订触发整份文件快照重建。
- [x] Inspector 开关保持同一张 Table/Grid 视图树；动画期间不按逐像素宽度重建，也不在 640pt 阈值切换滚动容器。
- [x] Inspector 正文与 Finder 标签按文件身份有界缓存并响应取消；同一文件二次展开不重复读取。
- [x] 浏览态复用 SwiftUI Table/LazyVGrid 虚拟化，冷启动与全量扫描完成不在主线程回读整个索引。

### 8B AI 文件理解与分类

- [x] OAuth 请求按 UTF-8 字节预算裁剪，任何 Provider 都不超过 Bridge 单段/总量门禁；界面明确显示截取范围。
- [x] “看文件”提供结构化摘要、关键事实、日期、风险与待办；“问文件”使用本地分块检索、片段引用并支持同文件连续追问。
- [x] 不支持格式、文件过大、扫描版 PDF、权限失效和模型空回复分别给出可恢复状态；不把生成失败误报为登录失效。
- [x] AI 分类支持 50 文件有界批量、置信度/理由、逐项编辑与跳过、本地规则兜底；AI 只使用已有分类，新分类由用户显式创建，应用可预览和撤销。
- [x] AI 分类默认仅发送文件名与类型，用户可显式开启“包含可提取正文”；所有索引文件类型均可参与分类。

### 8C 扫描范围与整台 Mac

- [x] 设置页同时保留“指定文件夹”和“整台 Mac（高级）”，切换不删除另一方授权或索引配置。
- [x] 整台 Mac 模式使用用户显式选择的启动磁盘安全作用域，并提供完全磁盘访问设置入口；不可读范围明确失败，不显示假成功。
- [x] 全盘由顶层受控 scope、批次提交与检查点组成，支持暂停、继续、取消与失败范围续扫；不把 `/` 作为单个原子快照一次载入内存。
- [x] 系统基础设施、缓存、应用包与高风险隐私目录默认排除；手动来源与全盘索引按激活模式隔离，不重复展示。
- [x] 全盘元数据优先且不启用根目录事件风暴；关闭全盘只停用其隐式 scope，不删除真实文件、手动来源或分类。

### 8D 在线更新与正式发布

- [x] 使用固定 Sparkle 2.9.5 的 SwiftUI/沙盒接入；设置与应用菜单均提供“检查更新…”。
- [x] GitHub Pages HTTPS appcast、EdDSA 密钥、Installer entitlement、首个 Universal 更新包、Developer ID 签名与 Apple 公证已闭环。
- [x] 自动检查可配置；未配置 feed/key 或关闭自动检查时启动零更新请求，Sparkle 负责呈现检查、最新版、新版本、网络与签名错误。
- [ ] 使用旧构建实测 Sparkle 升级、拒绝伪造包和失败恢复；v0.1.0 已生成公证 DMG，不重复生成。

### 8E 正式验收

- [ ] Instruments 记录首次选中、Inspector 开关、10 万项滚动、冷启动、扫描完成发布与峰值内存。
- [ ] VoiceOver、Reduce Motion、360/640/960/1200pt、列表/网格/分类页及更新状态完成实机验收。
- [x] 离线单元/协议/进程测试、5 万/10 万门禁和 arm64 Debug 构建通过；未经用户单独确认不运行真实 OAuth/AI、不生成 DMG、不提交。

### 8M 深度审查收尾（已完成）

- [x] 扫描取消、隐藏/排除文件对账与检查点提交保持数据安全；十万文件索引、选择、Inspector 与正文预览的主线程工作有界。
- [x] 命令面板、搜索、侧栏、整盘设置、分类工具栏、正文预览及网格 VoiceOver 在窄窗和键盘场景下完成收口。
- [x] Codex/Grok 使用独立 XPC connection lane；OAuth 业务请求与同 Provider 状态探针互斥，取消只影响对应 Provider。
- [x] Codex 状态读取在返回前关闭私有 Runtime；App 启用持久化 security-scoped bookmark entitlement。
- [x] OAuthBridge 66/66、OAuthProtocolClient 66/66，共 132/132、0 失败；未运行真实 OAuth/AI。v0.1.0 Universal DMG 已完成签名、公证、staple、Gatekeeper、Sparkle EdDSA 与完整性验证。

### 8F 页面层级、选择与 AI 交互收尾

- [x] 统一页面标题层级与所有文件摘要，分类详情保留返回全部分类入口。
- [x] 文件名称/网格选择即时反馈，Inspector 显隐不重建大列表布局快照。
- [x] 授权目录折叠与直接移除、AI Provider 整行展开、关于与更新合并。
- [x] AI 看文件/问文件稳定布局、居中加载、取消所有权与可读错误；AI 分类支持全选。
- [x] 菜单栏最近文件滚动、打开文件、回主窗口及应用命令恢复可用。
- [x] arm64 Debug 与 279 项聚焦测试通过；真实 Provider 与人工视觉验收仍由用户显式启动。

### 8G 列表性能、响应式与 Grok 流式修复

- [x] 分类详情同时保留原生样式的返回入口与右侧编辑分类入口。
- [x] 640pt 列表主体不再向父布局传播最小宽度；窄窗顶部搜索和工具栏按图标模式相同规则收纳。
- [x] Table 原生负责单击与多选，名称单元格仅叠加双击打开，避免大列表选中态迟到。
- [x] 文件索引按类型预分组，类型切换不再全库过滤；列表 24pt 图标改为有界 Quick Look 预览。
- [x] 分类详情筛选保留旧快照，类型切换不再显示错误的“正在搜索”；列表行补齐 VoiceOver 摘要与选中状态。
- [x] JSON-RPC 通知使用 65,536 条与 4 MiB 双重边界，解决 Grok `prompt.notification-overflow`，洪泛仍关闭连接。
- [x] 相关套件 112 项、最终通知预算 2 项均 0 失败；本地 Grok 完整 patch 只读复审 `APPROVED`。
- [ ] 使用当前构建人工验证窄窗、长列表滚动/点选、Inspector 与真实 Grok 看文件/问文件；未经确认不生成 DMG。

### 8H 窄窗操作与无障碍收尾

- [x] 批量操作栏使用连续响应布局；空间不足时只收纳动作，不压缩选择摘要或丢失功能。
- [x] Inspector 增加 720pt 最小呈现宽度；文件页可手动调整，过窄时稳定收起。
- [x] 废纸篓撤销横幅关闭按钮扩展至 28×28pt 命中区。
- [x] 重建搜索索引期间保留按钮文字，并向 VoiceOver 发布明确的运行状态。
- [x] 中文菜单术语统一为“在 Finder 中显示”。
- [x] Inspector 宽度门禁回归 1/1 通过；arm64 Debug 测试构建通过。

### 8I 名称列命中与选择性能收尾

- [x] 名称列移除单元格级双击、右键和拖拽命中层；原生 Table 负责整列单击与修饰键多选，Table/TableRow 分别承载主操作、上下文菜单和拖拽。
- [x] Table 本地选择与 AppModel 外部全选/取消双向同步；集合增量按显示顺序恢复 lead/anchor，键盘和菜单动作使用当前文件。
- [x] Table 画布使用当前 detail 宽度弹性布局，Inspector 显隐不保留旧固定宽度；列表与网格滚动位置统一 500ms 防抖写入。
- [x] Inspector 菜单按当前页面/宽度禁用，工具栏提供显隐选中态与 VoiceOver 状态；辅助列不重复朗读名称列整行摘要。
- [x] 隔离索引实点名称文字、缩略图和名称列空白区均即时选中，右键菜单可用；本地 Grok 名称列审查 `APPROVED`。
- [x] FileSelection、LagIsolation、NavigationModel、RemainingReview 共 96 项、0 失败；arm64 Debug 测试构建通过。
- [ ] Mac 解锁后继续人工复核 ⌘/⇧多选、Inspector 显隐与 360/640/960/1200pt；未经确认不运行真实 AI、不生成 DMG、不提交。

### 8J 原生 Table 响应式与分类编辑语义

- [x] 删除列表的 640pt 强制画布和裁切层，交还 macOS `Table` 处理弹性列宽与水平滚动。
- [x] 列表模式使用稳定布局身份；Inspector 显隐和 detail 宽度变化不再触发大 Table 重建，网格仍按列数响应。
- [x] 分类详情“编辑分类”改为原生菜单，提供“修改名称…”与 destructive“删除分类…”，复用既有 Sheet/确认框。
- [x] Apple 官方 `Table`、`inspector` 与 `NavigationSplitView` 文档完成核对；本地 Grok 补丁复审 `APPROVED`。
- [x] FileSelection、LagIsolation、RemainingReview 共 59/59、0 失败；arm64 Debug 测试构建通过。
- [ ] 当前构建人工复核 360/640/960/1200pt 水平滚动、Inspector 显隐帧率和分类菜单命中。

### 8K 文件浏览控件与状态反馈原生化

- [x] 搜索输入改用 AppKit `NSSearchField`，系统负责搜索图标、清除键、焦点环、键盘及辅助功能语义；现有防抖、历史和页面跳转逻辑不变。
- [x] 文件类型、排序与显示方式改用 SwiftUI 原生 menu/segmented Picker；过滤、AI、方向及 overflow 使用系统 Button/Menu，删除自绘工具栏表面与分段选中背景。
- [x] 多选操作区改用原生 `GroupBox + ControlGroup`，窄窗功能仍完整收纳；扫描、导出、撤销及数据库提示统一为系统 bar 材质和原生 ProgressView/Button。
- [x] arm64 Debug 测试构建通过；RemainingReview、FileSelection、LagIsolation 共 59/59、0 失败。
- [ ] 人工复核中英文、放大字号、360/640/960/1200pt 下的原生控件尺寸、菜单内容、搜索清除与批量操作命中。

### 8P 六位数文件库性能收口（已完成）

- [x] 20,000 项以上的列表/网格使用 `NSTableView`/`NSCollectionView` 数据源虚拟化；可见项缩略图、标签、选择、右键、拖拽、键盘与 VoiceOver 语义保留。
- [x] 页面局部原子快照、可取消稳定排序与单一工具栏布局消除全量 SwiftUI ID 收集和重复布局；网格几何在连续宽度/任意滚动位置保持唯一且不重叠。
- [x] 搜索状态与高亮从 `AppModel` 广播隔离，FTS 支持 SQLite progress handler 取消；独立 WAL 只读连接不再被批量写入阻塞。
- [x] 索引范围过滤、ID 派生移出主线程，无变化复扫不重建派生；增量 publication 竞态会回队重试。
- [x] 合并回归 113/113、最终网格 26/26、10 万门禁 1/1、arm64 Release/签名 Debug 构建均通过；真实 96,148 文件的列表/网格单击与 Inspector 采样无旧版约 1.1s 主线程布局栈。
- [ ] 真实 OAuth/AI、真实目录扫描、VoiceOver 与全宽度矩阵仍需用户显式验收；本阶段不提交、不生成 DMG。

### 8Q 原生控件生命周期与 OAuth 启动安全收口（实现完成）

- [x] 内置 Codex/Grok 四个 LFS Runtime 恢复为真实 Mach-O；XPC 构建前精确校验大小、架构、SHA-256 与官方签名，LFS 指针直接阻断构建。
- [x] 启动/回前台的 OAuth 状态探针只检查安全元数据与本地 proof，不再启动 Provider Runtime 或发模型请求；显式验证流程不变。
- [x] 原生列表/网格选择增加 AppKit→SwiftUI echo 所有权与当前 renderer 门禁；单击高亮不回退，切换显示方式不再被旧视图销毁事件清空。
- [x] 列菜单关闭自动验证、名称列不可隐藏；列偏好迁移至 v4，首次挂载默认隐藏创建时间/标签/位置，用户显式“全部显示”仍可持久化。
- [x] 侧边栏偏好与窄窗强制收起分离并带滞回；菜单使用当前窗口 FocusedSceneValue，搜索、视图切换、Inspector 与文件动作按上下文路由/禁用。
- [x] 文件时间戳 `NULL/0/1` 统一视为未知，避免系统占位时间显示为 1970；持久化 security-scoped bookmark entitlement 恢复。
- [x] 原生列 ID 统一归一化；首次默认隐藏创建时间/标签/位置，右键显隐使用短 ID 持久化并在 AppKit autosave 后应用。
- [x] 列表名称单元格与网格卡片的缩略图、文字、空白统一进入原生选择路径；普通、⌘、⇧与跨视图选择均有真实 AppKit 回归。
- [x] 合并离线回归 201/201、arm64 Debug 构建与深层签名验证通过；最终 QA 启动仅 XPC 存活、未启动 Codex/Grok Runtime。
- [x] 实机确认列默认显隐、跨列表/网格选择、菜单路由、搜索焦点、侧边栏偏好往返与 AI 未验证状态；首次实点发现并修正名称/网格子视图命中回归。
- [x] 最终第四版已在 96,148 条真实索引中实点名称文字、缩略图与网格卡片：选中即时迁移且等待 3 秒不回退；未运行真实 OAuth/AI，未提交、未生成 DMG。

### 8R 设计系统、适配与辅助功能收口（实现完成）

- [x] 将跨页面视觉 Token 与通用表面从搜索组件中独立，补齐字体、语义表面、尺寸、阴影、非活跃窗口与高对比度角色。
- [x] 命令面板补 Reduce Transparency 实体背景、紧凑高度适配与克制阴影；菜单栏搜索改用 min/ideal/max 约束。
- [x] 自定义卡片补非颜色选中信号、窗口失焦表现与更轻按压反馈；长文件名、路径及状态统一提供完整 Hover 提示。
- [x] AI Sheet 复用标题、尺寸、错误和操作区外壳；状态颜色同时保留图标与文字语义。
- [x] 保持 `AppLanguage` 单一入口并优先读取 String Catalog，现有手工英文值只作为缺失翻译回退。
- [x] 为设计系统核心组件补浅色/深色、状态及 Reduce Transparency Preview；安全拆分超大 SwiftUI 文件的纯视觉分区，不触碰 AppKit 数据源虚拟化与选择所有权。
- [x] 验收：XcodeGen、arm64 Debug build、App 363 项（2 项设计跳过）与 OAuth Process 38 项共 401 项、0 失败，`git diff --check` 通过；360/640/960/1200pt、VoiceOver 与放大字号保留为实机矩阵，不伪造人工结果。

边界：本阶段只在 `codex/ui-polish-0.1.2` 独立 worktree 实施，不修改 0.1.1 发布分支、已提交公证的 DMG、OAuth/AI、文件操作、索引或数据库逻辑。

### 8S 核心页面可见层级重塑（已完成）

- [x] 首页用克制的本地搜索导引面板建立第一视觉层级，最近文件与扫描位置使用紧凑空状态；文件类型仍保持独立可点击，不增加装饰性卡片。
- [x] 所有文件将工具区和空状态收拢为边界清晰的工作区，保留既有筛选、排序、AI、批量与列表/网格逻辑。
- [x] 分类总览增加用途说明和稳定的新建入口；设置增加导引区并限制为 760pt 可读宽度，窄窗仍自然回退。
- [x] 使用隔离临时数据库真实检查浅色/深色、窄/宽窗口与四个核心页面；发现并修正“新建分类”标题被压缩为图标的问题。
- [x] 验收：arm64 Debug build 成功；App 363 项（2 项按设计跳过）与 OAuth Process 38 项共 401 项、0 失败；`git diff --check` 通过。

边界：仅修改 SwiftUI 表现层和交接文档；不改 0.1.1 发布分支、版本号、DMG、OAuth/AI、扫描、文件操作、索引或数据库逻辑。

### 8T 文件检索可靠性与大库收口（实现完成）

- [x] 按“来源 + 监听根”持久化 FSEvents 游标；离线期间发生的变更在重启后回放，全盘多监听流互不掩盖，游标只在数据库更新成功且无待处理事件时提交。
- [x] 旧版本升级和无基线来源启动后自动完整补扫，包含已配置但仍为 0 文件的来源；整机分段断点不跨进程复用。
- [x] 移除生产全盘扫描的 10 万文件硬上限；扫描完成执行 WAL checkpoint/optimize，批量 FTS 更新不再逐 ID 全表扫描。
- [x] 搜索补齐文件名中间子串和符号名称，路径继续走全文索引，避免短词误命中系统临时路径。
- [x] 硬链接多路径分别入库，普通重命名身份稳定；完成后的迟到进度回调不会重新显示扫描中。
- [x] 扫描状态区区分“本轮已扫描”和索引总数，路径可读性提高。
- [x] 验收：App 401 项（2 项大型门禁按默认跳过）和 OAuth Process 38 项均 0 失败；显式 5 万/10 万门禁 2/2 通过；`xcodebuild analyze`、`git diff --check` 通过。

边界：未运行真实 OAuth/AI、计费请求或用户目录全盘扫描；未提交、未推送、未发布。

### 8U OAuth 最终登录体验（已完成）

- [x] 协议升至 v8，登录尝试显式携带登录方式、浏览器归属与回调模式；不匹配组合 fail closed。
- [x] Grok 主流程使用内置官方 Runtime `login --device-auth`，由 Runtime 消费完整授权 URL；寻简不解析日志、不注入浏览器环境或网页脚本。
- [x] Codex 保持 Browser OAuth + PKCE 自动回跳；Device Code 默认隐藏，仅可恢复失败后显示一次备用入口并原子切换 attempt。
- [x] 登录成功复用现有模型加载与选择恢复链路；空模型明确失败且不覆盖历史选择。
- [x] 设置页仅在真实 Device Code 展示存在时显示短码、复制与验证入口；没有粘贴验证码、WKWebView、Cookie/DOM/JavaScript 自动化。
- [x] 离线验收：主工程 418 项执行（2 项发布级大型门禁按默认跳过）与 OAuth Process 39 项，0 失败；Analyze、XML、Plist、源码安全扫描及 `git diff --check` 通过。
- [x] 真实 OAuth/UI 验收：Codex Browser OAuth + PKCE 自动回跳成功；Grok 官方完整短码授权 URL 登录、登出与重新登录成功；连接状态与模型列表均自动更新，未发送真实模型请求。外部授权页保留官方完成页，寻简不自动化关闭浏览器进程、窗口或其他标签。
- [x] 用户已确认提交、推送、升级版本并正式发布；0.1.7（build 8）发布流程已启动。

## 8V 当前代码审查修复（2026-09-05，用户已确认）

目标：修复本轮审查的七项问题及重复检测取消边界，不改界面结构、不升级依赖、不动 OAuth 协议、版本号和线上资产。

实施：使用 `test-driven-development`、`subagent-driven-development`，先验证失败，再最小修复，最后规格/质量审查；所有 Xcode 测试串行执行，测试只用隔离数据，不访问真实凭据或调用模型。

- [x] 索引恢复：`FileIndexCoordinator.swift`、`FileIndexInfrastructure.swift` 与 `NavigationModelTests.swift`；登记/串行处理在途事件，禁止游标越过未提交批次；正文刷新状态可跨取消和重启恢复。覆盖 A 阻塞/B 到达、失败重试、取消重扫、旧库迁移与正文事务回滚；同来源全扫等待增量结束，隐藏授权根补扫使用相对路径规则。
- [x] 取消一致性：`FileListExport.swift`、`DuplicateFileFinder.swift`、`StorageInsightsView.swift` 与 `RemainingReviewTests.swift`；取消传给实际后台任务，原子替换前检查，重复检测按 generation 隔离旧回调；取消与成功路径回归通过。
- [x] 预览性能：`TextPreviewView.swift` 与 `RemainingReviewTests.swift`；单向游标替代每次命中从全文起点计算距离并遍历全部分块；Unicode/跨分块、空查询、取消及20万字符高频匹配回归通过。
- [x] AI 状态隔离：`AISessionCoordinator.swift` 与 `OAuthBridgeTests.swift`；保存/删除 B 不覆盖 A 的测试/失败状态，重验失败清除旧验证记录；离线服务替身验证通过，三处旧用例改用隔离偏好。
- [x] 筛选边界：`AppModel.swift`、`AllFilesView.swift` 与 `FileSelectionTests.swift`；统一有限非负大小转换和恢复校验，类型条件可独立保存；极值/非有限值/负数/合法值及类型条件数据库回读回归通过。
- [x] 集成验收：最终完整离线回归主工程446项、OAuth Process 39项，共485项0失败；5万/10万门禁0.910s/6.475s通过；Analyze、XML/Plist、`git diff --check`及独立规格/质量审查通过。未提交、推送或发布。

风险与边界：正文恢复涉及持久化兼容性，需要旧数据库回读和重启恢复回归；导出只操作测试临时目录；真实升级/伪造包恢复、VoiceOver、长时间 Instruments 与真实 OAuth/AI 保留为独立验收，本轮不安装或替换已发布 App。

### 8W 双视觉主题与设置切换（已实现，专项验收通过，整壳手验待完成）

需求：保留已展示的 A「精密检索工作台」和 B「温润资料阅读室」，在设置 → 通用增加独立的「视觉主题」选择，不能仅添加无实际效果的开关。默认 B；主题与现有跟随系统/浅色/深色外观独立组合。

实施顺序与范围：

1. 复用现有偏好保存方式与设计系统，增加稳定主题标识和统一颜色、间距、阅读布局参数；无记录或未知值回退 B，不覆盖语言、外观、排序、显示方式等偏好。
2. 设置用原生选择控件展示两主题名称、简短说明及小型色彩示意；即时应用、自动保存、重启恢复，多窗口同步。不是将设计稿作为背景图片。
3. 两主题共用原生导航、工具栏和详情栏结构。A 冷白/深青、紧凑检索；B 暖白/朱红、舒展阅读。首轮重塑所有文件与详情栏，其他页面接入共享色彩；不另行重做首页、分类和 AI 流程。
4. 工具栏不跨越详情栏边界，标题无胶囊，保留原生菜单、路径与数量、细滚动条及窄窗回退。共享选择/搜索状态，切换主题不重新扫描、不丢查询与选中文件、不重置列表/网格偏好。栏宽尊重用户手动调整，不在每次切换时强制复位。
5. 设计稿中的文档内容仅为演示，PDF/图片尊重原文件；纯文本、代码预览保持适合内容的字体，不把所有文件套成衬线体。无可用正文时不伪造摘要或匹配原因；真实命中摘要若需扩展搜索数据接口，独立列出并验收，不夹带修改索引算法。

验收矩阵：A/B × 跟随系统/浅色/深色；旧偏好、未知主题值、退出重启、多窗口同步；宽/窄窗及双侧栏开关；空结果、长文件名、列表/网格、单选/多选；PDF/文本/代码/图片；键盘、VoiceOver、增加对比度、减少透明度；验证切换不触发索引/OAuth/AI、不清空当前搜索和选择。运行主题聚焦回归与必要构建，原生实机视觉验收未执行前不得标为完成。

执行清单（2026-09-05 已获用户确认）：
- [x] `AppVisualTheme.swift`、`XunJianApp.swift`、`SettingsView.swift`：默认 B、独立持久化及原生设置；基础策略先红后绿，真实双 `NSHostingView` 验证同步、状态身份保留及重建恢复。
- [x] `AppShellView.swift`、`SidebarView.swift`、`AllFilesView.swift`、`FileBrowseViewSupport.swift`、`FileLocationPathView.swift`：共享背景、合并工具区、原生路径与数量；主题刷新不更改数据身份。搜索框保留缩放高度。
- [x] `FileInspectorEmptyView.swift`：正文前置、原生信息/操作展开区；文本14/16号、代码等宽、PDF如实标注提取文本，原文件通过 QuickLook 查看。
- [x] `LargeFileTableView.swift`、`LargeFileGridView.swift`：背景/密度同步；十万行测试保持选择、顶部文件、列宽且无全量重载。
- [x] `MenuBarSearchView.swift`：独立快捷搜索根同步主题及应用外观，强制浅/深覆盖相反宿主外观。
- [x] Debug 构建、主题/交互专项测试、真实原生组件截图、XML/Plist/差异检查及独立规格/质量复核。
- [ ] 完整 AppShell/设置页手动操作、真实应用进程重启、键盘/VoiceOver、增加对比度/减少透明度验收。现有产品是单 Window，多主窗口不适用；双托管根同步测试不冒充实际多窗口功能。

验收记录（2026-09-05）：
- 最终主题专项15项通过（含快捷搜索反向外观像素断言），选择/导航/隔离/既有交互175项通过。Debug构建及XML/Plist/diff检查通过。
- 36张所有文件/详情组件截图覆盖 A/B×浅深色、280/420/620详情宽度、长名/代码/空选/多选、781/1241组件并排；另补4张快捷搜索反向外观截图。并排组件不是完整 AppShell；未运行真实 OAuth/AI。
- 设置仍复用原生 Form；截图夹具不进入会写标准通知偏好的设置整页。没有伪造命中摘要或将设计稿作为页面背景。
- 本轮全量497项中1个既有 OAuth 测试失败（2个断言）、2项性能测试默认跳过；单测重跑5次中1次失败。`OAuthBridgeTests.testLoginRestartsPollingAfterSleepingPollWasCancelled` 的旧轮询退出与新登录竞争，跨 Provider FIFO 替身多消费一次 Codex 响应；主题/SwiftUI 不参与该测试，OAuthCoordinator 本轮未改。单独登记处理，不能把专项通过写成全量通过。

边界：保留全部 8V 未提交修复，不修改 OAuth、文件操作、数据库、依赖、版本、CI 或发布资产；验收前不提交。

### 8X 整窗交互与 A/B 设计稿落实（本地复验通过，待用户视觉验收）

目标：用户2026-09-05明确批准全部修复，并提供A/B图要求忠实实现，不接受仅换色。两方案均采用品牌侧栏、单层工具区、多行真实文件结果、独立预览和稳定底栏；A分隔式结果/紧凑信息，B留白/柔和选择/更宽阅读区域。PDF呈现原文件，不能把设计稿正文套到用户文件上。本阶段不修改索引、OAuth、文件操作业务、版本或发布资产，不撤销8V/8W未提交修改。

架构：真实窗口验收发现 macOS 26 的 NavigationSplitView 自动浮动侧栏及顶区与稿不符，改为原生 HSplitView 平直三栏，复用 Sidebar、Inspector、列表/网格和现有状态存储；保留原生拖宽、红绿灯与显式边栏快捷键，不用截图背景实现主题。

诊断证据（2026-09-05）：
- AppShellView 的 setSidebarVisible 在强制折叠时忽略主动展开。直接执行当前状态结构：959 宽主动打开=false，放宽至1000=false，1021=true；NavigationModelTests 现有断言反而固化该行为。此为代码层确定复现，原生整窗点击仍需独立验收。
- AllFiles 工具区沿用旧断点并固定减去260宽，常用类型/排序/视图按钮过早进入更多菜单；A/B 结果结构相同，B 主要只增加行高。
- Inspector 身份、动作、正文、信息共用整个滚动区，长文把常用入口带走；内容预览从头截取，未提供已加载文本中的命中定位。
- 路径双击支持分段，外层右键却固定针对末端文件；分页按钮仅在顶部，筛选摘要未完整显示日期/大小。

推荐实施顺序（每步完成后验收，不并入未审查的新功能）：
- [x] 1. AppShellView + NavigationModelTests：主动展开左右栏优先于软折叠；同宽重复测量不再次折叠，主动关闭及页面切换保持意图。错误断言红→绿；900宽实机左栏主动展开通过，保留右栏硬最小宽度边界。
- [x] 2. AllFilesView + FileBrowseViewSupport：使用实际内容宽度；480pt起保留类型/排序/视图，窄栏搜索自适应，菜单扁平化；稳定结果/分页状态区，可移除筛选条件及空结果恢复入口。宽度策略红→绿。
- [x] 3. FileInspectorEmptyView + FileInspectorPresentationTests：紧凑身份和主要动作固定；正文独立滚动；信息/分类入口放底部且不遮挡正文。已有加载文本可定位命中，未加载部分如实提示，不扩展提取范围。空选容器撑满高度，关闭按钮固定顶部。
- [x] 4. AllFilesView + LargeFileTableView + NativeFileBrowserThemeTests：A/B默认多行结果，真实摘要/匹配类型/短路径；A紧凑分隔、B舒展柔和选择。属性表格/网格及用户列设置保留；修复模型已选中但原生列表未选中，四个完整窗口断言均为1项。
- [x] 5. FileLocationPathView：右键针对实际路径段；宽底栏单行路径+固定计数，窄栏两行稳定显示；临时文件覆盖祖先/末端目标，不操作用户文件。
- [x] 6. 首页、分类、设置及独立快速搜索：统一主题排版、对齐、字体层级、空态与焦点；设置中 A→B→A 实机切换通过，修复从设置返回侧栏键盘导航的边界。全部弹窗逐项交互未验收，不更改对应业务流程。
- [x] 7. ThemeLayoutSnapshotTests 扩充为完整 AppShell 验收，并在同一构建产物真实操作。孤立演示数据，不新增用户授权源、不做真实 OAuth/AI 验收；串行必要构建与聚焦回归，记录未测项。

验收矩阵：A/B×浅/深；720/900/1020/1200/1440宽度；左右栏按钮/菜单/快捷键、双栏展开、缩放往返、页面切换；长名/长文/空选/多选、列表/网格/大列表；查询与筛选保留、底栏路径菜单、分页后位置不跳；Tab焦点/键盘、VoiceOver、减少透明度/增加对比度。像素截图只作视觉证据，不代替点击验收；既有 OAuth 偶发测试失败不得冒充本次回归通过。

当前验收：166项专项通过（2026-09-05，run15）；最后空态高度修正后，14项预览/整窗专项及44张截图再次通过（run16）。A 预览占可用宽度44%、B 58%，原生分栏可拖宽。实机900宽左栏主动开、详情开关、主题A→B→A、设置跳转和快捷键通过。先测安全内容高度，再让各 pane 延伸至标题区，修复重复32pt留白、标题裁切与双侧栏关闭后高度塌缩。空选关闭按钮距窗顶由370pt修正为24pt，实机已复验。整窗必须等待真实 PDF 存在且几何稳定，再独立断言首页顶部，避免固定等待漏检；四窗均通过。plutil与git diff-check通过，无独立SwiftLint配置。全部弹窗、VoiceOver、辅助显示、多小时Instruments未验收；不宣称全软件无缺陷。

方案取舍：严格落实用户最新A/B图的结构，不继续只调颜色，不重写业务。先修交互再修结构和细节；验收前不提交，提交/发布另行授权。

### 8Y 操作体系与全页交互优化（已实施，专项验收通过）

目标：回应用户对侧栏开关、过小按钮和全局交互逻辑的不认可。8X的编译与布局通过不等于用户体验验收；保留A/B风格、原生三栏、真实文件预览与既有功能，本轮统一控件尺度、操作位置和状态反馈，不新增第三套主题。

只读证据：SidebarView:33–47的关闭入口位于品牌旁，AllFilesView:190–197的展开入口却在标题栏；AllFilesView:242–243隐藏全部菜单箭头，筛选与类型使用近似图标；FileInspectorEmptyView:182–203采用small打开按钮、无统一图标点击尺寸，210–236将操作藏在折叠信息后的滚动区；SettingsView:630对A主题整体缩小Form；分类文件页仍有返回/编辑、搜索、浏览多层工具区。实机辅助功能暴露左栏开关尺寸15×12pt（不等同于完整鼠标命中范围）。

推荐设计规则：
- [ ] 窗口控制：左侧栏开关放窗口顶部固定位置，展开/收起使用同一控件；右侧预览使用常驻、带状态的顶部开关，不藏进显示方式菜单。面板开合不改变筛选、选中、阅读位置，不抢搜索焦点；窄窗不可用时明确说明。采用原生控件，不重做浮动胶囊工具栏。
- [ ] 控件尺度：工具图标16–18pt，明确32–36pt点击区；常用文字按钮及搜索框统一32–36pt高度。A/B只区分排版密度，不缩小关键操作。统一hover、pressed、focus、selected、disabled及辅助标签；普通按钮用中性色，强调色仅用于选中和主动作。
- [ ] 文件页工具区：所有文件与分类文件使用一致的单层结构，标题旁保留“AI”文字入口；搜索始终可见；类型、排序、显示有明确名称或箭头。筛选合并为可辨识入口并显示生效数量，可单独移除和全部清除。窄窗先隐藏辅助文字，再合并次级菜单，不缩小点击区。
- [ ] 文件交互与预览：单击选择、双击遵循现有打开偏好；预览已展开时随选择更新，用户关闭后不强制重开。打开、快速查看、更多操作在两主题中有一致入口；信息折叠只承载元数据。多选显示明确批量动作与数量，危险操作不混入常用按钮组；保留原有确认和权限检查。
- [ ] 全页整理：导航分组与品牌留白适度收紧，路径栏优先显示可辨识名称而非一串小图标；首页区分未授权、扫描中、暂停、授权失效与无结果，各给一个恢复动作；设置增加轻量分区导航，保留表单状态和OAuth草稿；弹窗统一标题、取消/确认、错误与加载反馈。

范围与顺序：先AppVisualTheme/XunJianUI公共控件及AppShell/Sidebar；再AllFiles/FileBrowseViewSupport/Categories统一工具区；再Inspector/路径、Home/Settings/已有弹窗。仅按需求调整视图及相关状态同步，复用现有AppModel命令；不改索引算法、OAuth协议、数据结构、版本、CI、发布资产或用户文件。先验证侧栏与工具区一组代表窗口，再扩展全页，避免边实现边随意改变设计方向。

验收：A/B×浅深、左右栏四种组合、720/900/1440宽度及连续拖宽；按钮实际点击区、菜单状态、Tab/回车/Escape与现有快捷键；空选/单选/多选、加载/失败/无权限、长名称、中英文。检查关闭预览后选文件不会反复弹出，搜索和分栏不互相遮挡。专项测试覆盖行为变化；一次必要构建后用实际窗口点击和截图对照，不以通过数量替代体验验收。真实OAuth/AI、用户私有文件扫描不运行；辅助显示/VoiceOver未跑则明确标记。提交、推送、发布另需授权。

补充确定缺陷：SearchField.swift:111的onMoveSelection默认空操作，193–198仍吞掉方向键；上层SearchField没有传入该回调。实施时接入当前结果导航，并覆盖中文输入法组合态与无结果边界；未接入导航的场景不拦截原生按键。

确认：用户已整体批准本轮方案；按上述范围实施，不重复审批等效实现细节。键盘回归已确认旧实现2项测试、4处断言失败，修复后重验；验收前不提交。

2026-09-05实施结果：窗口控制、34pt公共按钮/菜单、文件与分类单层工具区、预览动作与位置恢复、首页五态、设置章节导航均已实现。上述复合验收项不以实现完成直接勾选：全部弹窗、辅助显示、VoiceOver及完整人工矩阵尚未完成，需继续保留边界。

验证：`/tmp/XunJian-8Y-verification3.log` 编译及186专项0失败，44张原生窗口/预览截图；最终仅状态链路改动以 `/tmp/XunJian-8Y-final-state-green.log` 复验134项0失败，未重复生成未变化的UI截图。原生borderless菜单即使外框34pt仍暴露14–17pt热区，已改button样式，实机AX高度34pt且距上缘2pt点击能展开菜单；左右开关34×34且开合位置稳定。实查720/900/1412窗口，设置章节方向键/回车使滚动位置由0变为0.23098。PDF与文本原生视图销毁重建位置探针先失败后通过，PDF恢复中再次关闭也已覆盖。

验收发现的保存搜索UI同步竞态最小修复：独立revision阻止旧reload/save/delete快照覆盖，独立session epoch阻止全取消后的列表/错误发布；数据库已提交操作不撤销，不改索引算法。受控真实数据库交错先5项3失败，取消/提前释放再2项5失败，最终均GREEN；测试gate记录释放终态，避免超时退出遗留挂起。保留全部原有改动，未提交、推送、发布或替换安装版。

### 8Z 原生操作框架与响应性能（已完成本地专项验收）

目标：用户否定8Y整体体验，已授权全面修复刚讨论的原生方向。将隐藏标题栏/自绘顶部/固定偏移入口改为系统窗口工具栏、NavigationSplitView与Inspector；保留A/B内容排版、原生表格和预览及既有业务。先移除窗口布局补偿，再迁移所有文件/分类工具项、缩减过重密度并对齐设置。卡顿以真实重复更新/窗口操作证据定位，不把Debug或测试数量当性能结论。

实现：系统titleBar/unified工具栏、NavigationSplitView与detail内部Inspector，删除布局轮询与固定偏移。Inspector仅注册一个工具项，左侧仅保留系统导航按钮；原生标题隐藏共享背景。文件/分类工具项留在主列，600pt以下合并次级菜单，搜索常驻；Inspector宽度为主列预留空间。A/B主题、原生表格/PDF、既有业务保持，收紧列表/导航密度并对齐设置。

验证：表格100次同主题更新先RED后GREEN，新增重绘请求从100降至0。最终 `/tmp/XunJian-8Z-native-final.log` 编译与125专项0失败，覆盖A/B明暗、720/900窄窗、搜索不越入预览、唯一侧栏/Inspector工具项及PDF位置；隔离夹具OAuth桥调用0。生成40张组件截图，不将其计作40张完整窗口验收。最新调试版实际冷启动并检查原生开关、搜索清除、分类、类型菜单、设置与主题切换，恢复B浅色及空查询。

边界：`/tmp/XunJian-8Z-current-sample.txt` 为10秒当前进程采样，主线程主要等待事件、内存88.8MB；与旧采样窗口状态、数据及宿主负载不同，不作量化帧率比较。大库长时滚动、macOS14/15回退、VoiceOver及完整全量仍未复测。设置自动读取现有连接状态，未执行真实登录/刷新/连接测试/AI生成或用户目录扫描。保留历史改动，不改索引算法/OAuth，不提交、推送、发布或替换安装版。

### 9A 全界面重设计（用户授权自主设计，本地专项验收完成）

目标：替换当前混杂的A/B页面布局，统一为紧凑、清晰、原生的文件工作台；不是再次换色。保留主题存储值、现有数据/索引/OAuth及文件操作确认，不提交发布。

设计：单一导航与页面骨架、系统字体、安静中性色；主题仅影响色温与阅读密度。分类统一紧凑目录行，名称/数量/操作同排，删除错误竖线；首页以搜索、最近文件为主，类型与来源次之；文件浏览延续原生虚拟化表格，预览统一文件身份与动作；设置及弹窗统一行宽、标题、按钮层级，保持草稿和焦点。

范围与顺序：先AppVisualTheme/XunJianUI及Sidebar，再Home/Categories/AllFiles/Inspector，最后Settings/AI弹窗/存储与快速搜索的共用样式。保持8Z系统工具栏及detail内部Inspector的已验证结构，不重写业务层。逐页检查空态、有数据、长标题、明暗及窄窗；分类新增真实渲染样本，旧主题结构断言按新设计更新，保留行为和窗口边界断言。编译、专项测试、实际窗口验收后交付；未测项如实列明。

实现：主题显示名改清晰/柔和，存储值兼容；页面骨架与系统排版统一。分类概览取消主题网格分歧，48pt紧凑行、同排数量、系统新建工具项；共享定高横线消除overlay中Divider方向错误。首页限宽840、最近文件单行身份/父目录简称/日期；文件浏览加入范围条、原生图标与日期统一；预览身份、AI/分类弹窗动作层级和存储页行宽统一。未改变索引、OAuth或文件操作业务规则。

验收：`/tmp/XunJian-9A-design-final.log` 为最终126项0失败及TEST SUCCEEDED；12主题/18Inspector/17原生表格/71导航/7页面结构/1多场景快照。覆盖主题明暗、首页/分类480与1100、窗口720/900边界、PDF阅读位置及单一工具项。新增横线回归并未执行独立先失败循环，不冒充RED→GREEN证据。第一次测试中分类结构断言范围过广，已限于概览，不影响详情页文件网格。

真实新版窗口验收：退出旧进程再启动专用DerivedData调试包；分类进入/返回、新建空名称禁用/输入启用/取消、搜索快捷键及清除、左右栏开合、设置章节到底部及双主题切换均通过。清晰浅色已恢复，无新增分类、用户目录扫描或AI生成；设置自身自动读取既有连接/模型。组件透明弹窗与PDF缓存图片不作完整视觉证据；分类弹窗实际窗口另验。大库长时性能、全部业务弹窗、VoiceOver、macOS14/15与全量测试仍未重跑。不提交、推送、发布、替换安装版。

### 9B 全产品体验重构（用户委托主导，已完成本地专项验收）

授权：用户撤销历史A/B及局部布局限制，由Agent主导UI、UX、交互、现有功能路径与动效；保留用户数据、业务能力与确认门禁，不提交发布。本阶段交付真实可运行客户端而非设计稿。

设计主线：查找→判断→操作。统一工作区以搜索为主；筛选与显示收拢，分类返回成为明确动作；预览负责阅读及文件操作。设置拆为通用、文件与索引、AI服务、关于四页，避免巨大长表单与跳转菜单；草稿使用既有独立Store保存。导航区集中目的地，存储洞察提供可发现入口。状态不包厚重卡片；短促反馈及折叠动画尊重Reduce Motion，不动画化整张结果表。

顺序：先设置的信息架构与回归，再统一文件/分类工具栏、首页与导航、预览及弹窗/快速搜索，最后动效与全流程验收。覆盖现有全部主要页面；不重写已验证的索引、数据库和OAuth协议。保留原生Table/PDF/NavigationSplitView边界，避免再次引入布局循环。

验收：设置切页/草稿、搜索与清除/筛选、分类返回、新建取消、文件选择/预览/菜单、侧栏、快速搜索/命令面板/存储入口；明暗、窄宽窗口、空态/真实夹具/错误状态。编译和专项回归、真实窗口操作、视觉复查完成后交付，未运行性能与平台验证明确列出。

实现：设置四页由Shell保留选择，Form切页从顶部显示，Provider草稿仍由独立Store保存；官方账号与API Key采用直接切换面板，切换不改变当前连接。首页管理位置、未配置AI入口分别直达文件/AI设置。文件页统一类型/大小/日期筛选，显示与排序同组；宽列AI独立，窄列合并次级操作，分类新增明确返回。导航补存储概览与命令入口，命令面板显示键盘提示，快捷搜索主动作与Return一致；预览动作及折叠、公共hover/pressed反馈遵循Reduce Motion。

回归：设置导航结构先RED；真实NSHosting窗口验证编辑模型字段→离开AI页→返回仍恢复草稿，OAuth桥调用0。独立AI工具项曾导致窄窗搜索越界50pt，按实际主列宽度收纳后 `/tmp/XunJian-9B-compact-green.log` 编译及126项0失败，含双主题明暗、480/900设置、720/900原生分栏与PDF阅读位置，并输出组件样本（不冒充整窗截图）。实机发现仅类型筛选时面板清除禁用，已统一清除类型/大小/日期并保留关键词；按钮辅助名称及命令条目重复朗读一并修正，`/tmp/XunJian-9B-interaction-green.log` 78项复验通过。

键盘收尾：实机发现命令面板打开未接管焦点；复用NativeSearchField而非增加固定延迟。Enter在筛选未完成时等待当前查询，不执行旧条目；新输入或关闭取消待提交。`/tmp/XunJian-9B-command-focus.log` 新增原生窗口回归通过，覆盖背景输入框→打开自动聚焦→Escape→重开→输入后立即Return执行最新设置命令，隔离OAuth调用0。冷启动CUA复验一致；另实际检查设置直达/切页、筛选清除、分类返回/新建取消、存储进退、左右栏、搜索焦点及菜单。清除测试查询和条件，未保存分类、未扫描用户目录、未登录或生成AI。已打开最终调试版，未提交、推送、发布或替换安装版。

边界：原生组件和系统PDF合成日志仍有系统约束/Live Text警告，未将其当作业务错误或零警告结论；未完成大库长时帧率/内存、macOS14/15、VoiceOver及全量业务验收。现有索引/OAuth历史边界保留，不声称全软件零缺陷或性能量化改善。

### 9C 重新定义产品工作空间（已完成本地专项验收）

授权：用户明确否定9A/9B沿用旧设计，并将全产品UI、UX、交互、现有功能组织与动效设计委托给Agent。9B的视觉方向不再作为本轮约束；测试通过不等于设计目标完成。不提交、推送、发布、替换安装版，不改用户数据、账号协议及索引引擎。

方向：寻简作为搜索驱动的资料工作台，而非Finder换肤。统一石墨/冷白、克制蓝色强调与系统字体，移除A/B外观选择；旧持久值兼容。三个主要空间：工作台（主搜索、最近工作、类型捷径和来源状态）、资料库（宽搜索带、下层筛选与显示、原生结果区、底部状态）、资料集（可检索的项目封面目录而非拉满横向的长行）。侧导航强调任务，保存的搜索及资料集作为快捷入口。预览阅读与信息采用清晰模式切换，操作固定底部；管理中心使用独立导航与内容区，窄窗保留直接入口。所有AI表单、命令/快捷搜索和存储使用统一的标题与操作层级。

顺序/范围：AppVisualTheme/XunJianUI及NavigationDestination、Sidebar/AppShell；Home、Categories、AllFiles；Inspector与Settings；AI公共框架/Storage/MenuBar/Command；相关结构、导航、原生分栏与草稿/焦点回归。复用虚拟化NSTable/PDF和现有业务服务。只在导航状态或用户展开时做短促可中断反馈，结果刷新和滚动不做大范围动画。

验收：三个空间必须有明显不同的新布局和完整可用入口；无旧主题选择；类别搜索/返回、文件检索/清除/选择/预览、设置切换及草稿、命令输入/取消、原生工具栏不越过预览边界；明暗/窄宽/空态/夹具。编译、专项测试、实际窗口截图与操作验收；未测项独立列明，不以测试数量替代视觉验收。

实现：以上工作空间、统一主题、导航式管理中心、单 Provider 配置、阅读/信息模式与底部文件操作已落地；原生 PDF 在模式切换时保持挂载。搜索带与预览分列，菜单使用较大原生按钮，440pt 以下保留图标及辅助说明。独立审查发现资料集 ⌘F 跳到资料库，导航回归先1项2失败，新增独立搜索 scope/原生焦点与查询保留验证后通过。实机又发现外部封面导航不更新左侧高亮，已将选择值显式传入行视图；冷启动从封面进入后高亮正确。

证据：`/tmp/XunJian-9C-final-checked.log` 编译及129项0失败，含明暗、480/900管理中心、720/900原生分栏、草稿/焦点/PDF位置，隔离 OAuth 桥调用0；最终菜单修改用 `9C-control-verification.log` 11项及全部布局样本复验，导航收尾用 `9C-navigation-final.log` 98项复验。长摘要320/600/800宽度新增回归 `9C-result-content-width.log` 通过，没有复现组件缓存截图中的裁切，未据此改写列表算法。上述短名日志均位于 `/tmp/XunJian-` 前缀下。

实机：最新 Debug 冷启动检查工作台、资料集检索/清除/封面导航/类型选择恢复、资料库预览开合与分列、管理中心、原生导航开关、命令聚焦及即时提交。清除测试查询与类型过滤，无新增资料集、用户目录扫描或真实 OAuth/AI 操作；不替换安装版。组件缓存截图不代表系统 PDF 合成效果；大库长期性能、macOS14/15、完整 VoiceOver、英文极窄窗口及全量业务仍未复验，不声称全软件零缺陷。未提交、推送或发布。

### 9D 检索与阅读台（结构已实现，键盘门禁未通过）

9C 的常驻深色侧栏、卡片首页和封面目录被用户否定，不再作为设计约束。重构为内容优先的原生检索与阅读台：启动直接查找，顶部直接切换查找/最近/资料集，资源目录按需展开；最近页是文件与位置记录，资料集是紧凑目录，设置用顶部原生分区，不再增加左侧导航。取消宣传标题和大型背景卡片，统一中性色、系统字体及适中的原生操作尺寸。文件检索、虚拟化列表、阅读/信息、账号配置、保存搜索及文件动作保持现有业务语义；短促反馈尊重 Reduce Motion，不对结果刷新做整页动画。

顺序：先固定新导航与结构验收，再改 Shell/主题、最近/目录/设置、检索和阅读操作；最后编译、导航/选择/草稿/原生布局专项回归、隔离夹具及真实窗口检查。保留既有未提交工作；不提交、推送、发布、替换安装版，不修改用户数据与索引/OAuth 协议。完成后记录当前证据，不把历史通过当成本轮验收。

9D 当前验收：最终构建及 94 项主题/导航/结构测试通过（`/tmp/XunJian-9D-toolbar-final.log`）。原生布局夹具通过（`9D-final-ui.log` 中 `testNativeThemeLayoutSnapshots`），涵盖明暗、窄宽、PDF 保持挂载、资料集查询重挂载；不要把同一日志的命令测试失败遗漏。实机复现并修复目录查询丢失、空索引恢复入口遗漏、768 点工具栏溢出；540 点模式菜单、阅读区展开关闭、管理位置跳转、查询跨页保留已复验。独立只读复审未发现剩余 P1/P2 静态问题。

未通过项：`9D-command-sized.log` 的命令面板键盘测试 1 项 2 失败，窗口未取得 key、Escape 后搜索控件仍在。夹具尺寸/前台激活三次调整仍未解决，按 systematic-debugging 暂停进一步试改，需讨论独立前台 UI 测试宿主。实机确认焦点后的 Esc/重开/回车通过，但一次紧连打开、粘贴、回车未到目标页，仍保留时序疑点；不宣称整体完成。无真实 OAuth/AI、用户扫描、Git 提交发布。9D 设计技能用于重排导航及页面层级，而非仅调色。

### 9E 全界面重构（用户已否决视觉方向，停止）

9E 蓝本原型已改动展示层，但用户于 2026-09-06 明确认为更丑，停止继续铺开与完成验收。`docs/DESIGN-9E.md` 和构图仅作历史记录，approved 已撤销。本地改动保留，未提交发布；索引、OAuth、数据和原文件规则不变。组件回归131项通过，首次原生布局夹具通过，不代表用户验收。新方向先交付完整黑白编辑台构图，收到视觉认可后再实施，避免重复盲改。

## 风险

- 外部应用进行的跨来源移动仍可能获得新身份；寻简内置“移动到”已迁移分类与搜索索引，文件身份仍由卷与资源标识决定，不引入版本系统。
- 内容哈希只用于用户显式触发的“查找重复文件”：先按大小分组，只对可能重复的组做 SHA-256 流式分块计算；大文件不静默跳过，读取失败整次明确失败且不返回部分哈希。结果不落库，也不参与文件身份或同步。
- Office 文档解析与 OCR 不在当前范围；单文件提取上限 8MB，索引正文上限 20 万字符。
- 5 万/10 万文件门禁与 96,148 文件实机交互采样已通过；长时间连续滚动的 p95 帧率与多小时内存稳定性仍需 Instruments 发布验收。
- 在线 Provider 未使用真实 API Key 做计费请求；设置页提供显式“测试连接”，不把“密钥已保存”冒充“已连接”。
- Qwen 当前推荐地域 Workspace Base URL；默认兼容地址可编辑，用户需与 API Key 所属地域保持一致。
