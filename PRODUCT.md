# 寻简

<!-- impeccable:product-schema 1 -->

## Platform

ios

实际交付平台为原生 macOS；此工具的 ios 平台族代表 Apple 原生界面，不能据此改成手机布局。

## Users

需要在 Mac 上查找、阅读、分类本地文件的人。当前用户明确要求整个软件的 UI、UX、交互及动画重新设计，并将设计选择交由实现者主导；不再继承旧 A/B、9C、9D 风格。

## Product Purpose

在授权范围内建立本机文件索引，按名称与已提取正文检索，原位预览和打开文件；分类只建立关联，不移动原文件。

## Capabilities and Constraints

现有能力：本地检索、类型/日期/大小筛选、列表及网格、最近修改、保存搜索、分类、阅读/文件信息、文件操作、扫描来源与排除、索引状态、AI 账号配置及用户主动触发的 AI 操作、菜单栏搜索、命令面板、存储概览。能力以现有调用链为准，不虚构 OCR、云同步或自动整理。

本次可重构整个展示层与交互；保留数据库、用户文件、权限授权、索引/OAuth 协议、连接凭据和已有业务动作。没有提交、发布或替换安装包授权。验证用隔离夹具，不运行真实 OAuth/AI，不扫描或修改用户文件。

## Brand Commitments

名称寻简，简体中文默认，英文与深色外观仍可用。旧视觉方案均已被用户否定，不作视觉约束。原生桌面操作、键盘、窗口缩放与性能必须保留。

## Evidence on Hand

现有 SwiftUI/AppKit 源码、用户提供的失败界面、专项测试与隔离文件夹具。演示数据只用于明确标注的设计图和测试；实际软件不填充虚假文件或统计。

## Product Principles

- 找到、判断、打开文件是一条连续任务，不在导航间反复跳转。
- 位置、筛选、当前选择与操作对象必须明确。
- 清晰的原生行为承载有辨识度的设计，不用装饰替代内容。
- 全部页面共享布局、控件、反馈和动效语法。
